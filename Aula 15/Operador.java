//Operadores especiais
public class Operador{

	public static void main(String[] args) {
		
		int idade = 18;

		String confirmação = (idade >= 18) ? "Maior de idade" : "Menor de idade"; // Nesse, "?" significa "então.." e ":" "Se não.."
		System.out.println(confirmação);
	}
	
}