public class Operador {
	public static void main(String[] args) {

		double a = 2 + 2; //soma, com valor fracionado
		double b = 2 - 2; //subtração
		double c = 2 * 2; //multiplicação
		double d = 2 / 2; //divisão
		double e = 2 % 2; //modulo(resto da divisão)
		int f = (int) ++a; // pre-incremento, como se fosse 1 + a
		double g = a++; //pos-incremento, ele so vai incrementar 1, na proximo linha
		double h = --a; // pre-decremento,como se fosse a - 1
		double i = a-- //pos-decremento, 
		System.out.println(/*Coloque uma variavel aqui para testar*/);

	}
}